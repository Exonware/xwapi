# XWUI + Riot.js

Static HTML with [Riot.js](https://riot.js.org/). XWUI Custom Elements work alongside Riot.

**Run:** From this folder: `npx serve -l 3015`. Open http://localhost:3015.

**Setup:** Run `node examples/sync-common.js` from repo root.
