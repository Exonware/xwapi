# XWUI + Nuxt

[Nuxt 3](https://nuxt.com/). XWUI Custom Elements in `app.vue`.

**Run:** `npm install` then `npm run dev`. Open the URL shown (e.g. http://localhost:3000).

**Setup:** Run `node examples/sync-common.js` from repo root so `public/` has XWUI assets.
