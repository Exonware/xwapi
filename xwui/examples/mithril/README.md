# XWUI + Mithril

[Mithril](https://mithril.js.org/) with Vite. XWUI Custom Elements in a Mithril view.

**Run:** `npm install` then `npm run dev`. Open the URL shown (e.g. http://localhost:5181).

**Setup:** Run `node examples/sync-common.js` from repo root so `public/` has XWUI assets.
