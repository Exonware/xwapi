# XWUI + Dojo

Static HTML with [Dojo](https://dojotoolkit.org/). XWUI Custom Elements work alongside Dojo.

**Run:** From this folder: `npx serve -l 3019`. Open http://localhost:3019.

**Setup:** Run `node examples/sync-common.js` from repo root.
