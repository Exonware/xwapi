# XWUI + Stencil

Static demo for [Stencil](https://stenciljs.com/) (Ionic). Stencil apps are Web Components; they can use XWUI Custom Elements the same way.

**Run:** From this folder: `npx serve -l 3023`. Open http://localhost:3023.

**Setup:** Run `node examples/sync-common.js` from repo root.
