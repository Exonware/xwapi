# XWUI + Next.js

[Next.js](https://nextjs.org/) App Router. XWUI Custom Elements in a client component.

**Run:** `npm install` then `npm run dev`. Open http://localhost:3000 (or the port shown).

**Setup:** Run `node examples/sync-common.js` from repo root so `public/` has XWUI assets.
