# XWUI + Marko

Static HTML with [Marko](https://markojs.com/) (eBay). XWUI Custom Elements work alongside Marko; this demo uses static HTML.

**Run:** From this folder: `npx serve -l 3016`. Open http://localhost:3016.

**Setup:** Run `node examples/sync-common.js` from repo root.
