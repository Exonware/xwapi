# XWUI + Hyperapp

Static HTML with [Hyperapp](https://hyperapp.dev/) (functional UI). XWUI Custom Elements in a Hyperapp view.

**Run:** From this folder: `npx serve -l 3018`. Open http://localhost:3018.

**Setup:** Run `node examples/sync-common.js` from repo root.
