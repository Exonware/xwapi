# XWUI + Stimulus

Static HTML with [Stimulus](https://stimulus.hotwired.dev/) (Hotwired). XWUI Custom Elements work alongside Stimulus controllers.

**Run:** From this folder: `npx serve -l 3011`. Open http://localhost:3011.

**Setup:** Run `node examples/sync-common.js` from repo root.
