# XWUI + Aurelia

Static demo for [Aurelia](https://aurelia.io/). In a full Aurelia app, load XWUI assets and use `<xwui-button>` in templates.

**Run:** From this folder: `npx serve -l 3021`. Open http://localhost:3021.

**Setup:** Run `node examples/sync-common.js` from repo root.
