# XWUI + Knockout.js

Static HTML with [Knockout](https://knockoutjs.com/) (MVVM). XWUI Custom Elements work alongside Knockout.

**Run:** From this folder: `npx serve -l 3017`. Open http://localhost:3017.

**Setup:** Run `node examples/sync-common.js` from repo root.
