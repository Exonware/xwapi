# XWUI + htmx

Static HTML with [htmx](https://htmx.org/). XWUI Custom Elements work alongside htmx.

**Run:** From this folder: `npx serve -l 3010` (or any port). Open http://localhost:3010.

**Setup:** Copy common assets into this folder: `node examples/sync-common.js` from repo root.
