# XWUI + Gatsby

[Gatsby](https://www.gatsbyjs.com/) (React static). XWUI Custom Elements in a page; assets in `public/`.

**Run:** `npm install` then `npm run dev`. Open http://localhost:8000.

**Setup:** Run `node examples/sync-common.js` from repo root so `public/` has XWUI assets.
