# XWUI + Backbone.js

Static HTML with [Backbone.js](https://backbonejs.org/). XWUI Custom Elements in a Backbone View.

**Run:** From this folder: `npx serve -l 3014`. Open http://localhost:3014.

**Setup:** Run `node examples/sync-common.js` from repo root.
