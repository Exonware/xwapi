# XWUI + Remix

[Remix](https://remix.run/). XWUI Custom Elements in a Remix route.

**Run:** `npm install` then `npm run dev`. Open the URL shown.

**Setup:** Run `node examples/sync-common.js` from repo root so `public/` has XWUI assets.
