# XWUI + Ember

Static demo for [Ember](https://emberjs.com/). In a full Ember app, load `xwui-button.mjs` and CSS, then use `<xwui-button>` in Handlebars.

**Run:** From this folder: `npx serve -l 3020`. Open http://localhost:3020.

**Setup:** Run `node examples/sync-common.js` from repo root.
