# XWUI + jQuery

Static HTML with [jQuery](https://jquery.com/). XWUI Custom Elements work alongside jQuery.

**Run:** From this folder: `npx serve -l 3013`. Open http://localhost:3013.

**Setup:** Run `node examples/sync-common.js` from repo root.
