# xwui project-specific ignores (all xwui ignores MUST live here, not in repo root)
# examples/ is tracked in git; only per-example build outputs are ignored below.

_Archieve/
.cursor/
.vscode/

# Node / build artifacts
node_modules/
dist/

# Example app outputs (examples/ is in git; exclude only build artifacts per subfolder)
examples/*/node_modules/
examples/*/dist/
examples/*/.next/
examples/*/.nuxt/
examples/*/.cache/
examples/*/public/

# Local tooling / caches
*.log
*.tmp
*.sqlite3

