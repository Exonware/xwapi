{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "XWUIInputDateRange",
  "name": "XWUI Input Date Range Component",
  "description": "Date range input component combining XWUIInput with XWUIPickerDateRange. Provides a text input field with a calendar popup for selecting a range of dates. Supports full view (inline calendar) and minimized view (popup calendar).",
  "path": "input.daterange",
  "tags": ["input", "date", "range", "picker", "calendar", "form", "desktop", "mobile", "tablet"],
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "conf_comp": {
      "type": "object",
      "title": "Component Configuration (conf_comp)",
      "description": "Component-specific settings set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "format": {
          "type": "string",
          "description": "Date format string for display (e.g., 'YYYY-MM-DD')",
          "default": "YYYY-MM-DD"
        },
        "separator": {
          "type": "string",
          "description": "Separator string between start and end dates in display",
          "default": " ~ "
        },
        "view": {
          "type": "string",
          "enum": ["full", "minimized"],
          "description": "View mode: 'full' shows calendar inline, 'minimized' shows calendar as popup",
          "default": "minimized"
        },
        "showIcon": {
          "type": "boolean",
          "description": "Whether to show calendar icon",
          "default": true
        },
        "iconName": {
          "type": "string",
          "description": "Icon name for the calendar icon",
          "default": "calendar"
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder text for the input field",
          "default": "Select date range"
        },
        "readonly": {
          "type": "boolean",
          "description": "Whether the input is read-only",
          "default": true
        },
        "clearable": {
          "type": "boolean",
          "description": "Whether to show clear button",
          "default": true
        },
        "className": {
          "type": "string",
          "description": "Additional CSS class name"
        }
      }
    },
    "data": {
      "type": "object",
      "title": "Component Data",
      "description": "Data to update the component, set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "value": {
          "description": "Selected date range value (array of two Date objects or strings)"
        },
        "minDate": {
          "description": "Minimum selectable date"
        },
        "maxDate": {
          "description": "Maximum selectable date"
        }
      }
    }
  }
}

