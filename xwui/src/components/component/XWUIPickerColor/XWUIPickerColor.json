{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "XWUIPickerColor",
  "name": "XWUI Picker Color Component",
  "description": "Color picker popup component - the overlay/popup part for color selection. This is the picker component that displays a color picker interface for selecting colors. Extends XWUIPicker base class. Typically used with XWUIInputColor or standalone.",
  "path": "picker.color",
  "tags": ["picker", "color", "popup", "overlay", "desktop", "mobile", "tablet"],
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "conf_comp": {
      "type": "object",
      "title": "Component Configuration (conf_comp)",
      "description": "Component-specific settings set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "format": {
          "type": "string",
          "enum": ["hex", "rgb", "hsl"],
          "description": "Color format for output",
          "default": "hex"
        },
        "presets": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of preset color values (hex codes) to display"
        },
        "placement": {
          "type": "string",
          "enum": ["bottom", "top", "left", "right", "bottom-start", "bottom-end", "top-start", "top-end"],
          "description": "Placement of the picker relative to trigger element",
          "default": "bottom"
        },
        "offset": {
          "type": "number",
          "description": "Distance in pixels from trigger element",
          "default": 8
        },
        "closeOnClickOutside": {
          "type": "boolean",
          "description": "Whether to close picker when clicking outside",
          "default": true
        },
        "closeOnEscape": {
          "type": "boolean",
          "description": "Whether to close picker on Escape key",
          "default": true
        },
        "className": {
          "type": "string",
          "description": "Additional CSS class name"
        }
      }
    },
    "data": {
      "type": "object",
      "title": "Component Data",
      "description": "Data to update the component, set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "description": "Selected color value (hex, rgb, or hsl string)"
        }
      }
    }
  }
}

