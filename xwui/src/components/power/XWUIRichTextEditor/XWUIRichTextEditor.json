{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "XWUIRichTextEditor",
  "name": "XWUI Rich Text Editor Component",
  "description": "Rich text/WYSIWYG editor component using contenteditable. Supports toolbar with formatting options, placeholder text, readonly mode, and configurable text formats. Useful for text editing and content creation.",
  "path": "input.richtexteditor",
  "tags": ["input", "editor", "wysiwyg", "rich-text", "form", "desktop", "mobile", "tablet"],
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "conf_comp": {
      "type": "object",
      "title": "Component Configuration (conf_comp)",
      "description": "Component-specific settings set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "toolbar": {
          "type": "boolean",
          "description": "Whether to show formatting toolbar",
          "default": true
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder text for the editor"
        },
        "readOnly": {
          "type": "boolean",
          "description": "Whether the editor is readonly",
          "default": false
        },
        "formats": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of allowed text formats (e.g., ['bold', 'italic', 'underline'])"
        },
        "className": {
          "type": "string",
          "description": "Additional CSS class name for the rich text editor"
        }
      }
    },
    "data": {
      "type": "object",
      "title": "Component Data",
      "description": "Data to update the component, set when initializing the component",
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "description": "Current HTML content of the editor"
        }
      }
    }
  }
}

