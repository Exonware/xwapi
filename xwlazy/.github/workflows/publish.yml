# NOTE: AUTO-GENERATED FILE
# This workflow was generated by tools/ci/python_scripts/generate_workflows.py.
# Do NOT edit this file manually - changes will be overwritten.

name: Dual PyPI Publish - exonware-xwlazy & xwlazy

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v0.0.2, v1.0.0, etc.
  workflow_dispatch:  # Allows manual trigger of the workflow

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Verify tag matches version.py
      run: |
        python .github/workflows/verify_tag_matches_version.py

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Set release date to today
      run: |
        python .github/workflows/update_release_date_from_version_file.py

    - name: Build exonware-xwlazy package
      run: |
        echo "Building exonware-xwlazy package..."
        python -m build

    - name: Build xwlazy package
      run: |
        echo "Building xwlazy package..."
        cp pyproject.toml pyproject.backup.toml
        cp pyproject.xwlazy.toml pyproject.toml
        python -m build
        mv pyproject.backup.toml pyproject.toml

    - name: List built packages
      run: |
        echo "Built packages:"
        find dist -name "*.whl" -o -name "*.tar.gz" | sort

    - name: Publish exonware-xwlazy to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: |
        echo "Publishing exonware-xwlazy to PyPI..."
        ls -la dist/
        twine upload dist/exonware_xwlazy-*.whl dist/exonware_xwlazy-*.tar.gz

    - name: Publish xwlazy to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: |
        echo "Publishing xwlazy to PyPI..."
        twine upload dist/xwlazy-*.whl dist/xwlazy-*.tar.gz

    - name: Success notification
      run: |
        echo "Successfully published both packages:"
        echo "   exonware-xwlazy v${{ github.ref_name }}"
        echo "   xwlazy v${{ github.ref_name }}"
        echo ""
        echo "PyPI URLs:"
        echo "   https://pypi.org/project/exonware-xwlazy/"
        echo "   https://pypi.org/project/xwlazy/"
        echo ""
        echo "Install with:"
        echo "   pip install exonware-xwlazy"
        echo "   pip install xwlazy"

