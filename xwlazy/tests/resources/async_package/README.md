# xwlazy-async-sample

Helper package installed during asynchronous benchmark runs. Provides a
single `ping()` function so correctness can be validated after the lazy
installer finishes pulling the dependency via pip.

