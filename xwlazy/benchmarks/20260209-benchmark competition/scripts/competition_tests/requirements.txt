# Requirements for competition benchmark tests

# Core dependencies
psutil>=5.9.0
memory-profiler>=0.60.0

# Optional: for better reporting
tabulate>=0.9.0
matplotlib>=3.5.0  # For visualization (optional)

# Note: The lazy import libraries themselves will be installed/uninstalled
# dynamically during benchmarking

